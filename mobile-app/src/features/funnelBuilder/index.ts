// Feature placeholder: Funnel Builder
// Planned: funnel list, funnel editor (likely webview or native block editor), forms + responses, domain verification status.

export type FunnelId = string;
export type FormId = string;
